---
import Layout from "../layouts/Layout.astro";
import { Clock, MapPin } from "lucide-react";
---

<Layout title="LDS FC | El Taller">
    <main class="max-w-6xl mx-auto px-6 pt-32 pb-20">
        <h1
            class="font-heading text-6xl md:text-8xl text-white uppercase mb-12"
        >
            El Taller
        </h1>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
            <!-- Schedule -->
            <div class="space-y-8">
                <p
                    class="text-xl text-neutral-300 font-light border-l-2 border-[var(--color-neon-cyan)] pl-4"
                >
                    "La fuerza verdadera no está en los músculos, está en la
                    decisión de volver a intentarlo."
                </p>

                <div class="bg-white/5 border border-white/10 p-8 rounded-sm">
                    <h3
                        class="font-heading text-2xl text-white mb-6 flex items-center gap-2"
                    >
                        <Clock className="text-[var(--color-neon-cyan)]" />
                        Horarios
                    </h3>

                    <div class="space-y-4">
                        <div
                            class="flex justify-between items-center border-b border-white/5 pb-4"
                        >
                            <span class="font-bold text-white">MARTES</span>
                            <span class="font-mono text-neutral-400"
                                >19:00 - 21:00</span
                            >
                        </div>
                        <div
                            class="flex justify-between items-center border-b border-white/5 pb-4"
                        >
                            <span class="font-bold text-white">JUEVES</span>
                            <span class="font-mono text-neutral-400"
                                >19:00 - 21:00</span
                            >
                        </div>
                        <div class="flex justify-between items-center">
                            <span
                                class="font-bold text-[var(--color-neon-cyan)]"
                                >SÁBADOS (Físico)</span
                            >
                            <span class="font-mono text-neutral-400"
                                >09:00 - 11:00</span
                            >
                        </div>
                    </div>
                </div>

                <div class="bg-white/5 border border-white/10 p-8 rounded-sm">
                    <h3
                        class="font-heading text-2xl text-white mb-6 flex items-center gap-2"
                    >
                        <MapPin className="text-[var(--color-neon-cyan)]" />
                        Ubicación
                    </h3>
                    <p class="text-neutral-400 mb-4">
                        Cancha Municipal "El Fortín"
                    </p>
                    <div
                        class="aspect-video w-full bg-neutral-800 rounded-sm flex items-center justify-center"
                    >
                        <span class="text-neutral-600 font-mono text-xs"
                            >MAPA PLACEHOLDER</span
                        >
                    </div>
                </div>
            </div>

            <!-- Philosophy/Signup -->
            <div
                class="relative bg-[var(--color-neon-cyan)] p-10 text-black flex flex-col justify-between clip-path-notch"
            >
                <div>
                    <span
                        class="font-heading text-4xl font-bold uppercase block mb-2"
                        >Sin Excusas</span
                    >
                    <p class="font-medium text-black/80 mb-8">
                        Buscamos compromiso. Si vienes a pasar el rato, este no
                        es tu lugar. Si vienes a dejar la vida, bienvenido a la
                        familia.
                    </p>

                    <ul
                        class="space-y-2 font-bold uppercase tracking-wider text-sm mb-12"
                    >
                        <li class="flex items-center gap-2">✓ Puntualidad</li>
                        <li class="flex items-center gap-2">✓ Respeto</li>
                        <li class="flex items-center gap-2">✓ Intensidad</li>
                    </ul>
                </div>

                <a
                    href="/contacto"
                    class="w-full py-4 bg-black text-white font-heading font-bold text-xl uppercase text-center hover:bg-neutral-900 transition-colors"
                >
                    Quiero Entrenar
                </a>
            </div>
        </div>
    </main>
</Layout>

<style>
    .clip-path-notch {
        clip-path: polygon(
            0 0,
            100% 0,
            100% calc(100% - 40px),
            calc(100% - 40px) 100%,
            0 100%
        );
    }
</style>
